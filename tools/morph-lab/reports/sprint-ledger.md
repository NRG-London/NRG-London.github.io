# SDD ledger — plan: C:\Users\neilg\.claude\plans\this-is-a-particularly-fizzy-leaf.md

## Pre-flight conflict scan

| Pair / task | Producer → consumer | Finding |
|---|---|---|
| M0 × M1 | M0 emits `d/oa.parents.json` `{"basis","n","parents":{lsoa,ward,pcon,borough,gla}}`; V0 `loadParents()` fetches it | Shapes agree across plan sections. Clean. |
| M0 × M2 | Same file consumed by V1 warm-up | Clean. |
| M1 × M2 | M1 code "transplanted verbatim" into V1, yet orchestrator is "renamed `morphTier(fromT,toT,m,fromVals,fromNd)`" with a different signature than V0's `morphTo(fromKey,toKey)` | Internal tension in plan wording. Ruling below. |
| M1 × M3 | V2 forks V0, reuses `paintFrom` with ward basis + `ward.labels.lad` as parentIdx | `paintFrom(basisT, parentT, …, parentIdx)` accepts any index array; lad[] fits. Clean. |
| M0 internal | Emits 5 parent links; V1 needs all 30 ordered pairs incl. oa endpoints | Orchestrator's `parentIdx===null` identity path covers oa endpoints. Clean. |
| M2 internal | `apply()` patch reads outgoing values before line-1753 reassignment | Verified against deployed file in planning. Clean. |
| M1/M2/M3 base | V0/V1/V2 copy the DEPLOYED index.html (build 23dcb3ae6b); lab `d/` copied from the same deployment | Self-consistent frozen snapshot; production republish mid-sprint cannot break the lab. Clean. |

## Rulings

- Ruling: helpers (`paintFrom`, `afterCommit`, `loadParents`, snap patch) transplant verbatim M1→M2; the orchestrator is expected to be ADAPTED (renamed + resignatured for apply() integration), not verbatim — the plan says both, adaptation is the intent. — Cost if wrong: small rework in M2.
- Ruling: work on branch `labs/morph` in the existing checkout, no worktree — user explicitly approved this deploy/branch model in planning, and `hugo server` must serve the tree the user will preview. — Cost if wrong: none material; tree was clean, work is purely additive.
- Ruling: user decision gates (M1/M2/M3 "judge by eye") are executed as OBJECTIVE self-verification during the run (crosswalk PASS suite, pixel-comparison of swap frames where possible, console-error-free load, HUD numbers), with all aesthetic verdicts deferred to the user in one sitting at the end — the user is not present mid-run and the plan's deliverable is a workbench they can judge. — Cost if wrong: if the user rejects style A at V0, the V1 patch work (which reuses V0 code) was premature; bounded waste, discardable by design.
- Ruling: commits on `labs/morph` add ONLY `static/labs/morph/**` and `tools/morph-lab/**`; `.superpowers/**` is never staged (repo .gitignore does not cover it and it must not ride a later merge to main). — Cost if wrong: stray scratch files in a commit; reversible.

## Progress

- Task 1: Ruling: brief's stated `lad[]` semantics were wrong (it indexes the tier's own alphabetical `ladNames`, NOT borough code-sorted rows; 5,158/26,369 differ). Implementer's name-translation validation accepted; reviewer independently re-derived the full crosswalk with 0 mismatches. — Consequence carried forward: ALL later tasks must consume `oa.parents.json` exclusively and never use `lad[]` as a borough row index (V2's planned direct use of `ward.labels.lad` must translate via ladNames→borough.names or equivalent). — Cost if wrong: none; validated two independent ways.
- Task 1: minor (deferred): `build_crosswalk.py` module-level mutable check bookkeeping — style nit in throwaway tooling, no action.
- Task 1: complete (commits c59aa9c..a9d0baf, review clean — spec ✅, quality Approved)
- Task 2: implementer DONE_WITH_CONCERNS at 2d05abc. PROVEN: plateau pixel-identity (MAD 0.0445/255, 0.034% px >12/255), exact endpoint landing (MAD ≤0.0003), boot clean. BROKEN: mid-flight interpolation (frames at 100.3% — snap paints used `transitions: {}` which drops deck's transition tracking). Driver: virtual-time budget unusable for animation; CDP-over-WebSocket used instead; needs unsandboxed shell.
- Task 2: Ruling: fix inside plan architecture — snap becomes ZERO-DURATION transitions (production's shipped curtain proves hidden seeding animates when the transitions prop stays populated; the empty object is the poison). Implementer's "always-visible basis layer" pivot recorded as V1 consideration only, NOT adopted (changes picking/tooltip semantics). — Cost if wrong: another fix round; pivot remains available.
- Task 2: fix round 1/5 dispatched (resume implementer): zero-duration snap, tessellation pre-pay, mid-flight progression assertions p25<p50<p75. Fix base 2d05abc.
- NOTE (environment): `content/_index.md` acquired uncommitted local modifications mid-sprint — the USER's own homepage edit (new census featured card), made in parallel. Never stage, never revert; explicit-path staging in all briefs protects it. Surface to user at handoff.
- NOTE (environment): headless Edge one-shot CLI (`--dump-dom`/`--screenshot`) currently no-ops on this machine; drivers must use `--browser` override to Chrome (works cleanly). Recorded in task-2-report Fix round 2.
- Task 2: fix round 2/5 (finding 1 driver error-assertions ADDRESSED — all 9 captures now gate on errors==0, no new breakage, re-run exit 0 numbers stable; finding 2 report reconciliation NOT ADDRESSED — pointer numbers one refresh behind; commits 35df68f..b3d6e8e)
- Task 2: fix round 3/5 dispatched — documentation-only reconciliation of task-2-report §1/§6 against committed RESULTS.txt.
- Task 2: fix round 3/5 (finding 2 report reconciliation ADDRESSED — verbatim block byte-identical to committed RESULTS.txt; doc-only, no commits)
- Task 2: minor (deferred, from task review): capture_v0.py `--duration` flag parsed but unused; mid frames sampled across separate page loads (cross-run monotonicity); 420×260 crop magic constant unasserted vs #panel bbox; V0 `plateau()` accepts non-basis b silently wrong; V0 `show()/plateau()` lack ready guards; `?at=` comment claims driver usage falsely; free_port TOCTOU + dead `op=` + missing server_close. (V1-relevant findings — finalise-snap guard, afterCommit shadowed param, watchdog, progression reference-frame — were promoted into the Task 3 brief as requirements instead.)
- Task 2: complete (commits a9d0baf..b3d6e8e, review clean after 3 fix rounds — spec ✅, quality Approved, morph proven sliding: progression 33.8→73.3→98.0%, endpoints ≤1 px, plateau MAD 0.0445/255)
- Task 3: implementer DONE at 67cb60f — 11/11 assertions, endpoints MAD ≤0.0004 uncropped, progression 7.71→54.13→95.03, patch surface 19 changed/642 added, curtain byte-for-byte. Found+fixed in V1: deck restarts in-flight transitions when polyLayer hands a fresh data.attributes object each redraw → `polyData(T)` per-tier cache keyed on paint counter. PRODUCTION TICKETS discovered (for user, out of sprint scope): (a) polyData restart bug — zoom during shipped curtain's FADE_UP restarts it; (b) rapid curtain switches: outer timer paints with reassigned CURRENT_VALS → NaN → TypeError, `switching` never clears (pre-existing); (c) RANKS cache ignores change view (pre-existing, no pixel effect).
- Task 3: review — spec ✅, quality Approved-with-issues: Important 1 = warm-up race can silently disable morph for the session while morphReady reports true (fix: only require B.painted when morphing FROM basis). Minors deferred: __v1.ready dead literal; SEED→ANIMATE window lacks destination baseline (measure change in ~160 ms window fades up); driver drift threshold 0.15 loose (tighten ~0.05); six pre-fix literals inside A10 block read as evidence; check() reads status before settle sleep; report "63 measures" (62); unconditional 100 ms status setInterval (gate on flag); __pycache__ not gitignored. Also noted: V0 lacks the polyData fix (zoom mid-morph in V0 restarts transition) — V0 is a frozen proven harness, triage at final review.
- Task 3: fix round 1/5 dispatched (resume implementer): morphReady race one-liner + report concern-3 recharacterisation. Fix base 67cb60f.
- Task 3: fix round 1/5 (both findings ADDRESSED — morphReady predicate fix empirically verified in both directions by re-reviewer; A12 state-probe accepted as sound substitute for race probe; no new breakage; commits 67cb60f..ff6ca18). Minor (deferred): report cites :2349 for the curtain TypeError, actually :2364-2365 post-fix.
- Task 3: complete (commits b3d6e8e..ff6ca18, review clean after 1 fix round — spec ✅, 12/12 assertions, endpoints ≤0.0004/255 uncropped, progression 7.77→54.82→94.99, curtain byte-for-byte + reachable via ?morph=0/street/not-warm)
- Task 4: spike verdict NEGATIVE at 7833cef/cd6afa1 — declaring a getPolygon transition renders NOTHING (WebGL `beginTransformFeedback: not enough transform feedback buffers bound` — deck can't bind TF buffers for the layer's fp64 vertexPositions; identical under Float32 input since the ATTRIBUTE is declared fp64), errors==0 throughout (caught only by new `ink` did-anything-draw measure + CDP logs). In-place position swaps work perfectly (0.0002/255). Choreography built but cuts. Ward→borough mapping via crosswalk: 689/689, 0 conflicts.
- Task 4: Ruling: one bounded continuation — CPU-driven per-frame position animation (rAF loop rewriting the ward position buffer through the warp curve; the plan's spike-fail branch explicitly allowed a pivot, and the user's decision was "Both — compare them"; a shader extension remains out of scope). Timeboxed: measure fps at ward grain; < ~15 fps → commit numbers and stop. — Cost if wrong: one bounded dispatch on a discardable page.
- Task 4: PRODUCTION-RELEVANT finding for handoff: attribute transitions on getPolygon are not merely unanimated here — they BLANK the layer with zero errors; any future transition work needs a did-anything-draw check (capture_v2's `ink`).
- Task 4: continuation RESULT (97c1948): CPU warp POSITIVE — 41.9 fps (689 wards / 50,738 verts re-warped+retessellated per frame; 2.8× the 15 fps floor), spike assertion passes on CPU path (8.14→55.43→94.91), split/merge end-states 0.0000/255. Measured constraint: values CANNOT animate concurrently with per-frame position rebuilds (deck transition buffer supersedes per-frame writes; concurrent lags; removed blanks) → choreography is three sequential beats (~1.1 s split). Scaling: style B is ward↔borough only (LSOA 7×, OA 38× per-frame cost).
- Task 4: review — spec ✅ (two disclosed deviations accepted: beat-relabelled controls; three-beat choreography forced by measurement), quality Approved-with-issues. Fix round 1 dispatched: (1) three hardcoded concern-1 numbers printed as if run-measured — attribute or restore a repro flag + %% literals; (2) afterCommit armed AFTER the redraw it waits on in the crack path (works only via snap-keepalive + watchdog) — reorder or justify. Minors deferred: driver claims boot layout assertion that doesn't exist (:80); beat()/direct() error-gate reads status pre-settle; report stale choreography/interrupt prose + S4 number drift; crack+6 comment vs crack+3/4 logs; hardcoded feature counts in a driver header.
- Task 4: fix round 1/5 (both findings ADDRESSED — S6 live concurrent-arm probe reproduces 29.2% lag independently; wrong "3.77/255 short" figure visibly corrected in 3 places; afterCommit reordered, crack+3 watchdog-free; re-reviewer ran the driver independently: exit 0. fps honest range ~25–46 across runs, floor 15 clear; commits 97c1948..fd1c97c). Minor (deferred): "at the 79% mark" fixed label vs fluctuating conc_at (79–85%).
- Task 4: complete (commits ff6ca18..fd1c97c, review clean after 1 fix round — built-in transitions NEGATIVE proven at mechanism level; CPU warp POSITIVE, three-beat choreography, ward↔borough only)
- Task 5 (added, plan "Done" definition): hub page checklist + variant summaries. Dispatched.
- Task 2: fix round 1/5 RESULT (commit 35df68f): PROGRESSION 33.55→72.70→97.72% (morph slides), endpoints 0–5 px, plateau 0.0445/255. Learnings for V1: SNAP_MS=1 (deck 9 treats duration:0 as UNSET → full-length animation); afterCommit must wait TWO commits (created ≠ applied); destination layer needs snap at FINALISE (hidden-painted layer's first draw otherwise fades up); residual 211 ms worst-frame at seed (basis attr upload on visibility flip); progression metric has ~24% floor from borough outlines flipping on at ANIMATE (metric artifact). Implementer withdrew always-visible-basis pivot. Proceeding to full task review (base a9d0baf).
