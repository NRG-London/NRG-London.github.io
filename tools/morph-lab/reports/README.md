# Sprint reports

These are the verbatim implementation and verification reports, plus the controller ledger (`sprint-ledger.md`), from the August 2026 morph prototype sprint on branch `labs/morph`, preserved here for reference during the eventual production port. The three production tickets identified for the port live in `task-3-report.md`, and the deck.gl transition-blanking finding (`getPolygon` transitions silently blanking the layer) is documented in `task-4-report.md`.
